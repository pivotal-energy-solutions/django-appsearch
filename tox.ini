# Tox (https://tox.readthedocs.io/) is a tool for running tests
# in multiple virtualenvs. This configuration file will run the
# test suite on all supported python versions. To use it, "pip install tox"
# and then run "tox" from this directory.

[tox]
envlist =
  {py27}-django{111}
  {py35,py36}-django{111,20,21}
  {py37}-django{20,21}
  pre-commit

[cleanup]
commands =
  find {toxinidir} -type f -name "*.pyc" -delete
  find {toxinidir} -type d -name "__pycache__" -delete
  find {toxinidir} -type f -path "*.egg-info*" -delete
  find {toxinidir} -type d -path "*.egg-info" -delete

[testenv:pre-commit]
skip_install = true
deps = pre-commit
commands =
  pre-commit run --all-files --show-diff-on-failure
  {[cleanup]commands}

[testenv]
passenv = TRAVIS TRAVIS_*
whitelist_externals =
  find
setenv =
  PYTHONWARNINGS=once::DeprecationWarning
  GIT_AUTHOR_NAME = "test"
  GIT_COMMITTER_NAME = "test"
  GIT_AUTHOR_EMAIL = "test@example.com"
  GIT_COMMITTER_EMAIL = "test@example.com"
commands =
  coverage erase
  coverage run demo_app/manage.py test --noinput --settings=demo_app.settings_test app_search
  coverage report -m
  pre-commit install
  {[cleanup]commands}
deps =
  django111: Django>=1.11,<2.0
  django20: Django>=2.0
  django21: Django>=2.1,<2.2
  -rrequirements.txt
  -rrequirements_dev.txt

[travis]
python =
  2.7: py27
  3.5: py35
  3.6: py36
  3.7: py37

[flake8]
max-line-length = 120
max-complexity = 10
ignore = F403,F405
exclude =
  */migrations/*
  */tests/*
  manage.py
  setup.py
per-file-ignores = strings.py:E501

[tool:isort]
lines_after_imports = 2
known_first_party = app_search,demo_app
known_third_party = django,django_registration,mock,requests
multi_line_output = 4
add_import = logging
